# 待办与已知问题

这份文件记录**已知但尚未动手**的事项,避免在对话里说过就忘。
每条都写清:问题是什么、为什么会这样、打算怎么做、以及动手前需要先确认什么。

---

## 1. 代理需要手动启动,外壳不会拉起它

**状态**:已确认,未动手。

**问题**

任务档位与自动上下文压缩都在 `context-proxy.mjs` 里(因为请求级采样参数会盖住
服务端参数,所以只能在请求层覆盖)。但它是一个**独立进程**,要在 Model Stove
点「启动」之后手工跑一次 `node context-proxy.mjs`。

后果是设置面板经常显示 `连不上代理:ECONNREFUSED`,而二维码退回 `:8091` ——
那个地址上档位和压缩都不生效。功能本身没坏,只是没人启动它。

**打算怎么做**

让 Model Stove 在拉起 `llama-server` 之后顺带把代理也拉起来,停止时一起收掉。
`src/main.js` 里已有子进程管理(含按 PID 台账清理遗留进程),复用它即可。

**动手前要确认**

- 代理的启动/停止要不要跟服务生命周期绑定,还是独立开关?
  (绑定的好处是不会忘;坏处是用户没法只跑模型不开代理)
- 代理崩溃时要不要自动重启?外壳当前对 llama-server 不做重启,保持一致更简单。
- 端口冲突:8092 被别的东西占了怎么办(应当给出明确报错而不是静默失败)。

---

## 2. QQ 官方机器人桥接已写完但未部署

**状态**:代码完成,卡在公网地址这一步。

`qq-bridge.mjs` 实现了完整链路:接收官方 webhook → 解析 `@` 提问 → 调本地模型
→ 通过开放平台 API 回复。Ed25519 验签是**照官方算法写的**,并且与官方示例
逐字节核对过(seed 与公钥都一致)。

**卡点**:官方平台要求一个**公网 HTTPS 回调地址**,而本机只有回环地址,
需要额外常驻一个隧道工具(cloudflared / cpolar 之类)。投入产出比不匹配当时的
想法,所以搁置。

**已有两个免凭据的自测模式**,不需要公网也不需要真实 Key:

```
node qq-bridge.mjs --selftest       # 验证本地模型链路与队列
node qq-bridge.mjs --test-webhook   # 验证 @ 解析
```

**注意**:官方平台的 Token 鉴权已废弃,必须用 AppID + AppSecret 换 Access Token。
另外 `@` 消息在 `content` 里的真实格式还没用线上数据确认过,代码按
`<@!openid>` 处理;接上真 QQ 时开 `QQ_DUMP=1` 转储一条原始事件即可核对。

---

## 3. 长文本生成会在中途出现重复退化

**状态**:已复现,机理未完全查清。

压力测试(单次生成 16K 小说)时,前七章正常,写到第七章的对话部分开始循环:

```
念说:"你说什么?"
林晚舟说:"我说,我回来了。"
    ↑ 重复几十遍直到耗尽上下文
```

**已排除的**:采样参数。用同一段退化原文做前缀、五组不同参数各生成 600 token,
**全部正常**(单句最多重复 1 次)。所以不是"低温 + 无重复惩罚"这么简单 ——
我一开始这么判断,被实验否定了。

**目前的理解**:关键变量是**单次生成的长度**,不是参数。差 25 倍
(600 token vs 14,845 token)。可能的原因:低比特量化模型在超长生成下逐步失去
一致性;KV cache 用 `q4_0` 量化,上下文越长误差累积越明显。

**可用的规避**:分段生成。每次请求只写几千字,而且代理能在"继续"之间压缩前文,
两个机制正好配合。这也是目前唯一被证据支持的解法。

**值得做的**:把那段退化原文存成固定回归用例,换模型或改参数后能一键复跑对比。
目前它只存在于一次性的实验脚本里。

---

## 4. 文档里的 `<电脑IP>` 需要与实际一致

**状态**:小问题,顺手可改。

README 里多处写 `http://<电脑IP>:8092/`,而实际地址随网络切换而变
(热点时是 `192.168.137.1`,校园网时是 `10.91.12.66`)。设置面板会自动列出并
标出推荐项,但文档没法自动更新。若以后统一网络环境,可把具体地址写进去。
